<!DOCTYPE html>

<html lang="en">
  <head>
    <title>Tiny App</title>
  </head>

  <body>
    <% include partials/_header.ejs %>
    <a href="/u/<%= url.shortURL%>">http://tinyapp.com/u/<%= url.shortURL %></a>

    <form action="/urls/<%= url.shortURL %>" method="post">
      <p>
        <label for="long-url-input">long URL</label>
        <input id="long-url-input" name="newLongURL" value="<%= url.longURL %>">
      </p>
    <p>
      <button>Update</button>
    </p>
    <p><a href="/urls">All my tiny URLS</a></p>
    </form>
  </body>
</html>